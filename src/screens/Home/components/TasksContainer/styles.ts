import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#1A1A1A',
        flex: 1,
        marginTop: -24,
        zIndex: -1,
        padding: 24
    },
    separator:{
        height: 8,
    }
})